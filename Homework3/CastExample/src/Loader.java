public class Loader {
    public static void main(String[] args) {
        double d = 24.0 * 0.1; // Пока не понимаю почему результат таков какой есть
        double maxDouble = Double.MAX_VALUE;
        double minDouble = Double.MIN_VALUE;
        System.out.println("24.0 * 0.1 = " + d + "\nMax Double value = " + maxDouble + "\nMin Double value = " + minDouble);
    }
}
